# React Native packages

Canonical repo: **[github.com/NPM-Packages-Modules/react-native](https://github.com/NPM-Packages-Modules/react-native)**

Each subfolder is one npm package for React Native mobile apps.

```bash
npm install
npm run build --workspace=servbridge
```
